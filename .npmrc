; Metro (React Native / Expo) cannot follow pnpm's symlinked node_modules layout.
; The linker must be set at the workspace root — a package-level .npmrc is ignored
; when installing from the root, which leaves react-native-css-interop unresolvable.
node-linker=hoisted
enable-pre-post-scripts=true
