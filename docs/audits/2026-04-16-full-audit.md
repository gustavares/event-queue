# Full Project Audit — 2026-04-16

**Status:** In progress
**Design:** [docs/superpowers/specs/2026-04-16-full-audit-design.md](../superpowers/specs/2026-04-16-full-audit-design.md)
**Plan:** [docs/superpowers/plans/2026-04-16-full-audit.md](../superpowers/plans/2026-04-16-full-audit.md)

## Executive Summary

_Populated in Task 8 after all findings are collected._

- 🔴 Critical: —
- 🟡 Important: —
- 🟢 Nice-to-have: —

### Top takeaways

_Populated in Task 8._

## Findings

### Auth

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
| 🔴 | Auth | Edge case 3: Token-expires-mid-session flow is broken — `didAuthError` in the urql auth exchange checks for `extensions.code === 'UNAUTHENTICATED'` but the backend never emits that code; expired tokens silently result in `user = null` in context, so protected resolvers return null/empty data instead of triggering a session clear and redirect | `rn-app/lib/graphql/client.ts:21` and `backend/src/index.ts:81` | Have protected resolvers (e.g. `myEvents`, `me`) throw a GraphQL error with `extensions: { code: 'UNAUTHENTICATED' }` when `context.user` is null, so the urql `didAuthError` check actually fires and triggers `clearAuth()` | M |
| 🟡 | Auth | Error 1: Duplicate-email error message wording diverges from spec — spec requires `"An account with this email already exists"`, but `signup.service.ts` throws `"${email} already registered"`, which surfaces verbatim in the Alert | `backend/src/domain/auth/signup.service.ts:43` | Change the thrown message to `"An account with this email already exists"` | S |
| 🟡 | Auth | Edge case 2: Client-side empty-form guard is a truthy check, not a proper validation — `!email` passes whitespace-only strings through to the server; no format or minimum-length validation exists on the client before submission | `rn-app/app/(auth)/sign-in.tsx:18` and `rn-app/app/(auth)/sign-up.tsx:19` | Trim input values and add basic client-side checks (valid email format, password min length) before calling the mutation, matching the server-side Zod schema constraints | S |
| 🟡 | Auth | Edge case 4: Network error message shown raw — urql's `CombinedError.message` for network failures is a technical string (e.g. `"[Network] Failed to fetch"`); shown as-is in an `Alert`, which is not user-friendly and diverges from the spec intent of a "network error message" | `rn-app/app/(auth)/sign-in.tsx:25` | Inspect `result.error.networkError` separately and display a human-readable message such as `"Network error — please check your connection and try again"` | S |
| 🟡 | Auth | Pattern: Repository pattern — concrete class exported as `default`, interface as named export; patterns.md documents "factory function or object with CRUD methods" but implementation uses a class; the default/named split also makes import ergonomics inconsistent with every other module in the codebase | `backend/src/repositories/user.repository.ts:17` | Either align with the documented "factory function" shape, or update patterns.md to say "class or factory function"; also consolidate to named exports | S |
| 🟡 | Auth | Pattern: Module-level side effect — `dotenv.config()` runs at import time in `jwt.service.ts`, which means any module that imports this file triggers environment loading as a hidden side effect; also calls `process.exit(1)` at module scope if `JWT_SECRET_STRING` is missing, making the module untestable in isolation | `backend/src/domain/auth/common/jwt.service.ts:5` | Refactor to accept a config/secret argument in the exported functions, or initialise via an explicit `init()` call driven by the app entry point; document the convention in patterns.md under Backend Patterns | M |
| 🟢 | Auth | New pattern: Utility services named as nouns (`password.service.ts`, `jwt.service.ts`) — patterns.md specifies `<action>.service.ts` (verb) for use-case services; these are shared helper utilities living in `common/`, not use-case services, so the naming convention doesn't map cleanly | `backend/src/domain/auth/common/password.service.ts` and `backend/src/domain/auth/common/jwt.service.ts` | Document in patterns.md: files in `domain/<feature>/common/` are utility helpers and follow `<noun>.service.ts` naming to distinguish them from use-case services | S |
| 🟢 | Auth | New pattern: Store-level storage helpers — `auth.store.ts` defines `saveToken`, `getToken`, `deleteToken` as module-level functions co-located with the Zustand store; patterns.md Store Pattern says nothing about where persistence helpers live | `rn-app/stores/auth.store.ts:23-45` | Document in patterns.md that storage helpers may be co-located in the store file when they are used only by that store; consider flagging fire-and-forget `saveToken(token).catch(console.error)` at line 54 — a SecureStore failure silently diverges persisted vs in-memory state | S |
| 🟢 | Auth | New pattern: Entity type file co-located with repository — `user.entity.ts` sits alongside `user.repository.ts` in `repositories/`; patterns.md documents only "one file per entity" but doesn't describe entity type files or their `.entity.ts` suffix convention | `backend/src/repositories/user.entity.ts` | Document in patterns.md: entity type files use the `<entity>.entity.ts` suffix and live in `repositories/` alongside their repository file | S |
| 🟢 | Auth | New pattern: Error handling — services uniformly use `throw new Error(...)` and handlers re-throw with the same message; no error type hierarchy, no Result/Either types, no documented convention; patterns.md is silent on error handling strategy | `backend/src/domain/auth/signup.service.ts:35,42` and `backend/src/graphql/handlers/auth/signup.handler.ts:31` | Document the chosen convention in patterns.md ("services throw plain `Error`; handlers catch and re-throw to preserve the message to the GraphQL layer"); existing TODO comments in signup.service.ts also flag desire for custom domain errors — decide and document | S |

### Events CRUD

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
| 🔴 | Events CRUD | AC-12: "Reopen Event" is broken end-to-end — the reopen handler calls `handleTransition('DRAFT')`, but the spec + server allow only `FINISHED → ACTIVE`; the server correctly rejects this as "This event cannot be changed to that status", so the user can never reopen a finished event | `rn-app/app/(app)/events/[id]/index.tsx:82` | Change the `reopen` branch to `handleTransition('ACTIVE')` (and update the confirmation dialog copy "The event will be moved back to draft status" on line 119 to say "active" to match) | S |
| 🔴 | Events CRUD | AC-10/11/12/13: `transitionStatus` is invoked with the wrong variable name — the mutation expects `$id: ID!` but the client passes `{ eventId: id, status: ... }`; urql sends the request with an unused variable and a missing one, so every Publish / Close / Cancel / Reopen action fails with a GraphQL variable error | `rn-app/app/(app)/events/[id]/index.tsx:67` | Pass `{ id, status: toStatus }` to match the mutation definition at `rn-app/lib/graphql/operations/events.ts:99` | S |
| 🔴 | Events CRUD | AC-15: `deleteEvent` is invoked with the wrong variable name — mutation expects `$id: ID!` but client passes `{ eventId: id }`, so soft-delete never happens and the subsequent `router.replace('/(app)')` leaves the event present on the list | `rn-app/app/(app)/events/[id]/index.tsx:73` | Pass `{ id }` to match the mutation definition | S |
| 🔴 | Events CRUD | AC-29/30: `updateDoorSaleTier` and `removeDoorSaleTier` are invoked with wrong variable name — mutations expect `$id: ID!` but the edit screen passes `{ tierId: ... }`; GraphQL server rejects with a "Variable not defined" error, so tier updates and removals never reach the backend | `rn-app/app/(app)/events/[id]/edit.tsx:206`, `:213-215`, `:228` | Pass `{ id: tier.id, input: ... }` and `{ id: tier.id }` respectively; align all four mutation calls with their operation definitions | S |
| 🔴 | Events CRUD | BR-14 / Edge 7: Disabling door sales deletes all existing tiers instead of preserving them — the edit screen iterates `eventData.event.doorSaleTiers` and removes each via the tier mutation when `doorSalesEnabled === false`, directly contradicting "Disabling door sales does not delete existing tier configuration (preserved for reactivation)" | `rn-app/app/(app)/events/[id]/edit.tsx:226-229` | Remove the delete-all branch; tiers should only be mutated when `doorSalesEnabled === true`. Persisting the `doorSalesEnabled=false` flag alone is sufficient to mark door sales inactive | S |
| 🔴 | Events CRUD | BR-1: Event + EventTeamMember creation is NOT wrapped in a DB transaction — `CreateEventService.run` issues two separate awaits (event insert, then team-member insert); a DB error between the two calls (connection drop, unique constraint on `event_team_member`, validation race) leaves an orphaned event with no Manager, which breaks every downstream authorization check and makes the event unreachable via `myEvents`/`event` | `backend/src/domain/events/create-event.service.ts:58-74` | Wrap both inserts in a single Drizzle `db.transaction(async (tx) => { ... })`; pass `tx` into both repository calls (repositories may need an overload that accepts an external tx) | M |
| 🟡 | Events CRUD | AC-9 / BR-2-4: Editing an event to switch from venue → custom location sends `venueId: null`, but the zod schema in `UpdateEventService` is `z.string().optional()` which rejects `null` (only `undefined` passes); `schema.parse()` throws "Expected string, received null", so the whole save fails with no user-visible reason | `rn-app/app/(app)/events/[id]/edit.tsx:181` and `backend/src/domain/events/update-event.service.ts:27` | Either (a) change the client to send `venueId: undefined` when switching to custom, or (b) relax the schema to `z.string().nullable().optional()` and update the type; option (b) is preferred since the entity allows null | S |
| 🟡 | Events CRUD | AC-26/27 / BR-12: The edit screen never sends `doorSalesEnabled` in the `updateEvent` input — changing the switch in the UI changes local state but the field is omitted from the mutation payload, so toggling off/on after an initial value is set silently does nothing on the server | `rn-app/app/(app)/events/[id]/edit.tsx:171-176` | Include `doorSalesEnabled` in the `input` object passed to `updateEvent` | S |
| 🟡 | Events CRUD | AC-19: `GetEventsService.getById` admits any team member (checks only that `membership` exists), but spec AC-19 says "User can fetch a single event by id if they are its Manager"; today the creator is the only team member so behaviour is equivalent, but the permission check is looser than the spec and will silently admit promoters/hosts when Team Management lands | `backend/src/domain/events/get-events.service.ts:17-20` | Either tighten to `membership.role !== 'MANAGER'` to match the spec literally, or update the spec to allow any team member (the latter likely reflects actual product intent — flag for PO review) | S |
| 🟡 | Events CRUD | Edge 4 / Error row "Invalid status transition": Reopening a CANCELLED event returns the generic "This event cannot be changed to that status"; spec edge case 4 requires the specific message "Cancelled events cannot be reopened." | `backend/src/domain/events/transition-event.service.ts:40` | Add a dedicated branch before the generic guard: `if (event.status === 'CANCELLED') throw new Error("Cancelled events cannot be reopened.")` | S |
| 🟡 | Events CRUD | Error row "Missing required event field": Spec mandates a single rolled-up message "Please fill in all required fields", but zod's default refinements throw per-field messages like `"name: String must contain at least 1 character(s)"` which surface verbatim to the client | `backend/src/domain/events/create-event.service.ts:18-42` | Catch ZodError in the service (or handler) and translate to the spec's consolidated message; alternatively, add custom messages on each `.min(1, "...")` so the client can still show inline errors, and rely on client-side validation for the rolled-up copy | S |
| 🟡 | Events CRUD | Error row "Venue missing name or address": Spec expects "Venue name and address are required", but `CreateVenueService` throws "Input validation failed: name: Name is required; address: Address is required" by concatenating zod errors — message diverges and is not user-friendly | `backend/src/domain/venues/create-venue.service.ts:27-29` | Translate zod validation failures to the spec message; the current `CreateVenueSchema` already uses `"Name is required"` / `"Address is required"` — just throw a single pre-composed string matching the spec | S |
| 🟡 | Events CRUD | Error row "Event not found": Spec says "Event not found" is accompanied by "Redirect to events list"; the current detail screen renders the error inline with a manual "Go back" link instead of auto-redirecting to `/(app)` | `rn-app/app/(app)/events/[id]/index.tsx:136-145` | On `!event && !fetching`, call `router.replace('/(app)')` (with an optional toast/banner explaining the redirect) instead of rendering the inline dead-end | S |
| 🟡 | Events CRUD | AC-6/7/8: Server-side validation errors ("Please select a venue or provide a location", "End time must be after start time", "Start time cannot be in the past") never reach the user on the Create screen — `handleSubmit` checks `result.error`, sets `submitting=false`, and returns silently; the form sits with the button re-enabled and no feedback | `rn-app/app/(app)/events/create.tsx:131-134` and `rn-app/app/(app)/events/[id]/edit.tsx:187-190` | Surface `result.error.graphQLErrors[0].message` (or a translated/mapped version) via `setErrors({ submit: ... })` or a toast; the current silent failure violates the "stay on form" + "highlight missing fields" behaviour spec | S |
| 🟡 | Events CRUD | AC-6 / BR-4 (client-side gap): The Create form's client validator requires `locationName` in custom mode but not `locationAddress`, so users can submit with an empty address; the server catches this via the venueId-or-(name+address) refine, but per the spec both inline fields are required — the client should prevent the trip to the server | `rn-app/app/(app)/events/create.tsx:80-88` and `:[id]/edit.tsx:135-143` | Add a parallel `!locationAddress.trim()` check for custom mode; show an inline error on the address field to match the spec's "Please select a venue or provide a location" intent | S |
| 🟡 | Events CRUD | Pattern consistency: Door-sale-tier mutations rely on `updateEvent` for the `doorSalesEnabled` flag rather than a dedicated `enableDoorSales`/`disableDoorSales` mutation as the spec's user stories read; combined with the edit-screen bugs above this makes the door-sales flow fragile. Documenting this explicitly in the spec (or adding the two mutations) would stop the pattern being re-invented on each client | `backend/src/graphql/schema/index.ts:130-145`, `docs/features/events-crud/spec.md:84-85` | Either (a) add `enableDoorSales(eventId: ID!)` / `disableDoorSales(eventId: ID!)` mutations, or (b) amend the spec to note "Enable/disable door sales is done via the generic `updateEvent` mutation" | S |
| 🟢 | Events CRUD | AC-32 (client-side): Create / edit screens let users add tiers with empty name or empty price fields and silently skip them on submit (`filter((t) => t.name.trim() && t.price.trim())`) rather than showing a validation error, which contradicts the spec's "Please provide a tier name and a price greater than zero" inline-error expectation | `rn-app/app/(app)/events/create.tsx:139` and `:[id]/edit.tsx:211` | Validate tier rows before submit; either highlight incomplete rows or block the submit with an inline error. Also guard against `parseFloat` yielding NaN for non-numeric price input | S |
| 🟢 | Events CRUD | BR-12 (door-sales enable/disable path): `manage-tiers.service.ts` checks Manager role directly, but adding a tier when `doorSalesEnabled === false` is not blocked — a tier created while door sales is off will be persisted and surface whenever sales is re-enabled. Spec is ambiguous here (BR-14 says tiers persist across enable/disable cycles), but the current behaviour lets you configure tiers for disabled sales, which surfaces in the UI as rows with no "enabled" indicator | `backend/src/domain/events/manage-tiers.service.ts:22-42` | Either (a) accept as intentional and document, or (b) reject `addTier` when `event.doorSalesEnabled === false`; decide with PO | S |
| 🟢 | Events CRUD | AC-25: `getVenue` returns `null` when no venue matches — spec doesn't say it should 404, but handler swallows the nullability and exposes the raw null to clients with no error distinction; parallels `Event not found` handling | `backend/src/graphql/handlers/venues/get-venue.handler.ts:14-19` | Decide: either keep permissive null (spec allows it) or throw "Venue not found" for parity with Event handling. Low priority since no UI consumes this yet | S |
| 🟢 | Events CRUD | BR-1: `event_team_member` has a uniqueIndex on `(eventId, userId)` (db/schema.ts:55-57), but `EventTeamMemberRepository.create` has no conflict handling — not an issue for Events CRUD itself (one Manager per event), but foreshadows Team Management needing `onConflictDoNothing`/`onConflictDoUpdate` clauses | `backend/src/repositories/event-team-member.repository.ts:24-35` | Document in patterns.md (or future Team Management spec); no change needed for this feature | S |
| 🟢 | Events CRUD | Doc drift: `plan.md` references an `UpdateEventDbInput.status` field (line 185) that IS present in `event.entity.ts`, but update-event.service.ts never passes status through — status mutations go exclusively through `transition-event.service.ts`. The shared Update input works fine, but mixing status into the generic `updateEvent` handler would bypass the transition rules; current code relies on discipline, not type-level guarantees | `backend/src/domain/events/update-event.service.ts:67-76` | Omit `status` from `UpdateEventDbInput` (create a dedicated `TransitionEventDbInput = { status: EventStatus }`) to make the separation enforceable at compile time | S |
| 🔴 | Events CRUD | Pattern: Repository pattern — every new repository (`event`, `venue`, `door-sale-tier`, `event-team-member`) uses `export default class Drizzle...Repository implements XRepository`, repeating the drift already noted in Auth; patterns.md documents "factory function or object with CRUD methods", so the whole data-access layer now diverges from the documented convention | `backend/src/repositories/event.repository.ts:34`, `venue.repository.ts:24`, `door-sale-tier.repository.ts:24`, `event-team-member.repository.ts:21` | Either align all repositories with the factory-function shape, or update patterns.md to document "class-based Drizzle repositories implementing an interface"; consolidate default/named export split in the same pass | M |
| 🔴 | Events CRUD | Pattern: Handler pattern — `list-venues.handler.ts` and `get-venue.handler.ts` bypass the service layer and call `context.services.venueRepository` directly; patterns.md says "Handler: Calls the appropriate service", and the domain layer has no `list-venues.service.ts` or `get-venue.service.ts`. This also forced the `venueRepository` and `doorSaleTierRepository` to be injected into the `services` container (`graphql.types.ts:22,29`), mixing repositories and services in the same namespace | `backend/src/graphql/handlers/venues/list-venues.handler.ts:11-14`, `get-venue.handler.ts:11-14`, `backend/src/graphql/graphql.types.ts:22,29` | Add `list-venues.service.ts` and `get-venue.service.ts` (or a single `get-venues.service.ts` with `listAll`/`getById`) that the handlers delegate to; rename the context field from `services` to something honest (e.g. split into `services` and `repositories`) if repo injection is kept | M |
| 🔴 | Events CRUD | Pattern: Data access layer — the `Event.createdBy` type resolver in `resolvers/index.ts` constructs a raw Drizzle `select()...from(user)...` query inside the resolver file, bypassing `userRepository` entirely; patterns.md mandates "Data access layer (one per entity)" and resolvers must go through repositories to keep Drizzle isolated | `backend/src/graphql/resolvers/index.ts:53-60` | Add a `findById` method to `UserRepository` and inject it via context; call `context.services.userRepository.findById(parent.createdBy)` from the resolver instead of hand-rolling a Drizzle query in the resolver file | S |
| 🟡 | Events CRUD | Pattern: Service pattern — Zod validation is applied inconsistently across services: `create-event.service.ts:51` uses `schema.parse()` (throws raw `ZodError`), `create-venue.service.ts:25-29` uses `safeParse` and concatenates per-field errors, `manage-tiers.service.ts:32-34,59-61` uses `safeParse` but throws a hardcoded message; patterns.md says "Validates input with Zod schemas" but doesn't prescribe failure shape, so this drift is likely to propagate | `backend/src/domain/events/create-event.service.ts:51`, `create-venue.service.ts:25-29`, `manage-tiers.service.ts:32-34`, `update-event.service.ts:49` | Pick one convention (`safeParse` + a canonical thrown error) and apply uniformly; document in patterns.md under Service Pattern so future services don't re-invent | M |
| 🟡 | Events CRUD | Pattern: Handler error handling — drift within Events CRUD: `create-event.handler.ts:37`, `update-event.handler.ts:39`, `delete-event.handler.ts:18`, `get-event.handler.ts:15`, `list-events.handler.ts:14`, `transition-event.handler.ts:19` use `throw error instanceof Error ? error : new Error(String(error))`; `add-tier.handler.ts:18`, `update-tier.handler.ts:18`, `remove-tier.handler.ts:15` use `throw new Error(error instanceof Error ? error.message : "Failed to X")` (fallback strings lose the original error); `create-venue.handler.ts:27` uses a third shape with `error: any`; patterns.md is silent | `backend/src/graphql/handlers/events/*.handler.ts` and `backend/src/graphql/handlers/venues/create-venue.handler.ts:22-28` | Document a single error-handling convention in patterns.md (e.g. "handlers do NOT catch — let GraphQL Yoga surface the error; services throw `Error` with user-facing messages") and apply uniformly; the `try/catch/console.error/rethrow` in every handler is effectively a log-and-rethrow wrapper that adds no value | S |
| 🟡 | Events CRUD | Pattern: Handler auth gating — every handler re-implements `if (!context.user) throw new Error("Authentication required")` as its first line (9 event handlers + 3 venue handlers + 2 tier handlers = 14 copies); patterns.md defines "Receives GraphQL args and context" but does not document the auth-guard pattern. Besides the repetition, the thrown error has no `extensions.code: UNAUTHENTICATED` — same root cause as the Auth audit finding about urql's `didAuthError` never firing | `backend/src/graphql/handlers/**/*.handler.ts` (14 files) | Introduce a `requireAuth(context)` helper (or a `withAuth(handler)` wrapper) in `graphql/handlers/common/` that throws a typed `GraphQLError` with `extensions: { code: 'UNAUTHENTICATED' }`; document in patterns.md | S |
| 🟡 | Events CRUD | Pattern: Resolver wiring — `resolvers/index.ts` currently mixes flat `Query`/`Mutation` maps with a nested `Event` type-resolver object that also embeds business logic (the `Event.createdBy` Drizzle query, see 🔴 above); patterns.md says "Keep flat — no nested resolver objects unless needed for type resolution" which permits this but doesn't describe where type-resolver code should live when it grows. Today the file is 62 lines; at 5+ entities this will bloat | `backend/src/graphql/resolvers/index.ts:45-61` | Document a convention in patterns.md: "type resolvers for entity X live in `graphql/resolvers/<entity>.resolver.ts` and are re-exported from `resolvers/index.ts`"; extract `Event` resolvers to `event.resolver.ts` as the first application | S |
| 🟡 | Events CRUD | Pattern: Repository — soft-delete policy is inconsistent across repositories. `event.repository.ts:60,70` filters `deleted = false` in `findById`/`findByCreator`; `user.repository.ts` has no analogous filter even though `user.deleted` exists in the schema; `door-sale-tier.repository.ts` and `venue.repository.ts` have no soft-delete columns at all. Patterns.md is silent on soft-deletes, so the next entity will have to guess | `backend/src/repositories/event.repository.ts:60,70`, `user.repository.ts:37-57`, `backend/src/db/schema.ts:15,43` | Document in patterns.md the soft-delete convention ("repositories filter `deleted = false` in all read paths; hard-delete is only for transient entities"); audit `user.repository.ts` reads to apply the filter | M |
| 🟡 | Events CRUD | Pattern: Multi-table writes — `create-event.service.ts:58-74` performs two sequential `await` statements against two repositories (event insert, then event-team-member insert) without a transaction; same service also clearly SHOULD be transactional per the spec (see 🔴 BR-1 row already in this table). Patterns.md is silent on transactions, so this will be re-litigated whenever a new multi-table write lands (guest lists, etc.) | `backend/src/domain/events/create-event.service.ts:58-74` | Add a Transactions section to patterns.md: "Services that write to more than one table MUST wrap the writes in `db.transaction(async (tx) => {...})`; repositories must accept an optional `tx` parameter, defaulting to `this.db`." Refactor `event.repository.ts` + `event-team-member.repository.ts` to accept a `tx`, then wrap the create-event service in a transaction | L |
| 🟡 | Events CRUD | Pattern: Service pattern — services use a mixed `run(input)` (`CreateEventService`, `UpdateEventService`, `TransitionEventService`, `DeleteEventService`, `CreateVenueService`) vs explicit method names (`GetEventsService.getById`/`listByCreator`, `ManageTiersService.addTier`/`updateTier`/`removeTier`); patterns.md's Service Pattern prescribes "One file per use case" but doesn't say how many methods a service may expose. `GetEventsService` and `ManageTiersService` each cover multiple use cases, which either violates "one file per use case" or signals that the rule needs a "related reads/writes may share a service" exception | `backend/src/domain/events/get-events.service.ts`, `manage-tiers.service.ts` | Clarify in patterns.md: "A service file may expose multiple methods when they share the same aggregate and authorization rules; otherwise one method named `run()`"; or split `ManageTiersService` into `add-tier.service.ts`, `update-tier.service.ts`, `remove-tier.service.ts` to follow the `<verb>.service.ts` convention literally | S |
| 🟡 | Events CRUD | Pattern: Handler args typing — inconsistent `_parent`/`_args` typings: events handlers use `_parent: unknown` (typed), venue handlers use `_parent: any` + `_args: any`; within events, `update-tier.handler.ts:5` declares `args: { id: string; input: {...} }` inline, while `create-event.handler.ts:4-16` spells out the `input` shape fully. Patterns.md is silent on typing | `backend/src/graphql/handlers/venues/*.handler.ts:12,6-7` and `backend/src/graphql/handlers/events/*.handler.ts` | Document in patterns.md: "handlers type `_parent: unknown`, `args` with the operation's input shape, and `context: AppGraphQLContext`; no `any`". Align venue handlers to the events pattern | S |
| 🟡 | Events CRUD | Pattern: Context namespace — `AppGraphQLContext.services` mixes actual services (`createEventService`, `manageTiersService`) with repositories (`venueRepository`, `doorSaleTierRepository`) because of the service-layer bypass noted in the venue-handler row above; the field name no longer matches its contents, and new readers will wonder whether repositories are supposed to be in context at all | `backend/src/graphql/graphql.types.ts:17-30` | Split into `AppGraphQLContext.services` and `AppGraphQLContext.repositories` (or fold repositories behind new services); document the context-shape convention in patterns.md | S |
| 🟡 | Events CRUD | Pattern: Frontend screen self-containment — `create.tsx` (475 LOC) and `edit.tsx` (564 LOC) duplicate the venue-picker Modal verbatim (lines 365-472 in create.tsx ≈ lines 454-561 in edit.tsx), plus the inline create-venue form and all its state (`newVenueName`, `newVenueAddress`, `newVenueCapacity`, `showCreateVenue`, `venueSearch`, `venueModalVisible`). Patterns.md does not mention component extraction, so screen files will keep growing | `rn-app/app/(app)/events/create.tsx:364-473` and `rn-app/app/(app)/events/[id]/edit.tsx:453-562` | Extract a `VenuePickerModal` component into `rn-app/components/ui/` (or `rn-app/components/events/`) accepting `visible`, `onSelect`, `onClose` props; document in patterns.md: "Screens stay under ~300 LOC; shared modals/forms live in `components/`" | M |
| 🟡 | Events CRUD | Pattern: Frontend typing — pervasive `any` in `create.tsx:37,70,92,116,390,392`, `[id]/edit.tsx:57,99,112,124,125,194,212,226,479,481`, `[id]/index.tsx:212`, and `resolvers/index.ts:46,50,53`; patterns.md is silent on strictness. Urql operations are untyped, so every `result.data?.createEvent` etc. is `any`, masking the wrong-variable-name bugs already flagged in Task 4 | `rn-app/app/(app)/events/**/*.tsx` | Document in patterns.md: "Frontend code uses urql's generated types (consider `@graphql-codegen/cli` or `graphql-request`) — `any` is not allowed in `*.tsx` screens." Generate types from the schema and refactor | L |
| 🟢 | Events CRUD | New pattern: Drizzle schema conventions — columns use `snake_case` DB names with `camelCase` TS properties (`door_sales_enabled` ↔ `doorSalesEnabled`, `location_name` ↔ `locationName`, etc.); primary keys are `varchar(24)` with `$defaultFn(() => createId())`; booleans use `.default(false).notNull()`; timestamps use `.defaultNow().notNull()`; enums live at the top of `schema.ts`. Consistent but undocumented; newcomers will have to reverse-engineer | `backend/src/db/schema.ts:1-65` | Document Drizzle conventions in patterns.md under a new "Database Patterns" section (column naming, id/primary-key shape, default/null defaults, enum placement) | S |
| 🟢 | Events CRUD | New pattern: Entity files re-export `InferSelectModel` as `<Entity>Schema` alongside domain `<Entity>Entity` interfaces — `event.entity.ts:4`, `venue.entity.ts:4`, `door-sale-tier.entity.ts:4`, `event-team-member.entity.ts:4`; patterns.md doesn't describe this split (domain Entity vs DB Schema) | `backend/src/repositories/event.entity.ts:4-24`, `venue.entity.ts:4-14`, `door-sale-tier.entity.ts:4-12`, `event-team-member.entity.ts:4-14` | Document in patterns.md: "Entity files expose (a) `XSchema = InferSelectModel<typeof x>` for internal repo use and (b) a domain-level `XEntity` interface for handlers/services; repositories map Schema → Entity" | S |
| 🟢 | Events CRUD | New pattern: `mapToEntity` helper convention — every repository has a file-private `mapToEntity(row: XSchema): XEntity` function near the top (`event.repository.ts:14-32`, `venue.repository.ts:12-22`, `door-sale-tier.repository.ts:14-22`, `event-team-member.repository.ts:11-19`); patterns.md is silent on the mapping convention | `backend/src/repositories/*.repository.ts` | Document in patterns.md: "Each repository exposes a file-private `mapToEntity(row: XSchema): XEntity` used by every read method; add fields here rather than in the query builder" | S |
| 🟢 | Events CRUD | New pattern: Soft-delete columns follow a `deleted: boolean` + nullable `deletedAt: timestamp` shape (user + event tables); door-sale-tier and event-team-member are hard-deleted; patterns.md is silent on when to soft-delete vs hard-delete | `backend/src/db/schema.ts:15-18,43-46` | Document in patterns.md: "Aggregate roots (user, event, venue) soft-delete; owned child rows (door-sale-tier, event-team-member) hard-delete with the aggregate" (or whatever policy the team picks) | S |

### Cross-cutting

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
| 🔴 | Cross-cutting | Auth-error code never propagates to client — the backend's 14 handlers (auth + events + venues + tiers) all throw `new Error("Authentication required")` without `extensions.code = 'UNAUTHENTICATED'`, while the urql `authExchange.didAuthError` only clears session when that code is set. Root cause of Auth Edge Case 3 (🔴) AND the Events CRUD "handler auth gating" row (🟡). Every protected resolver in both features exhibits the same leak | `backend/src/graphql/handlers/**/*.handler.ts` (14 files) and `rn-app/lib/graphql/client.ts:21` | Introduce a `requireAuth(context)` helper in `backend/src/graphql/handlers/common/` that throws `GraphQLError` with `extensions: { code: 'UNAUTHENTICATED' }`; refactor all 14 handlers to use it; document the pattern in `patterns.md` under Handler Pattern | M |
| 🔴 | Cross-cutting | Repository pattern drift — all 5 repositories (`user`, `event`, `venue`, `door-sale-tier`, `event-team-member`) are `export default class Drizzle<X>Repository implements <X>Repository` with a named-export interface, but `patterns.md` documents "Exports a factory function or object with CRUD methods." Both features (Auth 🟡 + Events CRUD 🔴) drift identically, so the doc is the source of the drift, not the features | `backend/src/repositories/*.repository.ts` and `docs/patterns.md:35-39` | Pick one direction: either (a) rewrite `patterns.md` Repository Pattern to document "class-based Drizzle repositories implementing an interface" and consolidate default/named exports to a single style, or (b) rewrite all 5 repositories as factory functions. Also align export style (see `patterns.md` exports gap) | M |
| 🟡 | Cross-cutting | Zod validation-failure shape is inconsistent across features — Auth's `signup.service.ts:34-35` and `signin.service.ts:31-32` both use `safeParse` + concatenated per-field errors; Events CRUD has three distinct shapes (`create-event.service.ts:51` raw `parse()`, `create-venue.service.ts:25-29` concatenation, `manage-tiers.service.ts:32-34` hardcoded message). `patterns.md` Service Pattern says "Validates input with Zod schemas" but prescribes no failure shape, so every service re-invents its own | `backend/src/domain/auth/*.service.ts`, `backend/src/domain/events/*.service.ts`, `backend/src/domain/venues/create-venue.service.ts`, `docs/patterns.md:42-46` | Extend Service Pattern in `patterns.md`: "Use `safeParse`; on failure throw a single `Error` whose message matches the spec's Error Handling row for that operation." Refactor all services to the canonical shape; add a shared `parseOrThrow(schema, input, specMessage)` helper if the translation gets repetitive | M |
| 🟡 | Cross-cutting | Thrown error messages diverge from spec Error Handling wording in both features — Auth's duplicate-email throws `"${email} already registered"` vs spec `"An account with this email already exists"` (🟡); Events CRUD diverges on "Please fill in all required fields" (🟡), "Venue name and address are required" (🟡), and "Cancelled events cannot be reopened" (🟡). All surface verbatim to the user via `Alert.alert(...result.error.message)` | `backend/src/domain/auth/signup.service.ts:42`, `backend/src/domain/events/create-event.service.ts:18-42`, `backend/src/domain/venues/create-venue.service.ts:27-29`, `backend/src/domain/events/transition-event.service.ts:40` | Add a checklist item to `/po validate` (or a new spec-review step): for each spec Error Handling row, confirm the thrown error text in the service matches verbatim. Fix the four divergences in parallel with the Zod-shape refactor above | S |
| 🟡 | Cross-cutting | Raw `CombinedError.message` surfaced to users via `Alert.alert` — Auth's sign-in screen shows urql's `"[Network] Failed to fetch"` verbatim (🟡); Events CRUD's detail screen shows `transitionStatus`/delete errors raw in Alert (implicit across the 🔴 mutation bugs). No feature has a mapping layer between GraphQL errors and user-facing copy | `rn-app/app/(auth)/sign-in.tsx:25-26`, `rn-app/app/(auth)/sign-up.tsx:26-27`, `rn-app/app/(app)/events/[id]/index.tsx` (transition/delete flows), `rn-app/app/(app)/events/create.tsx:131-134`, `rn-app/app/(app)/events/[id]/edit.tsx:187-190` | Introduce a frontend `formatGraphQLError(error: CombinedError): string` helper in `rn-app/lib/graphql/` that distinguishes `networkError` from `graphQLErrors[0].message` and maps known codes to user copy; route all `Alert.alert(..., result.error.message)` sites through it. Document the helper in `patterns.md` Frontend Patterns | M |
| 🟡 | Cross-cutting | Handler-layer error handling varies across every handler in both features — Auth handlers throw plain `Error` re-throws; Events CRUD has three distinct catch shapes (`throw error instanceof Error ? error : new Error(String(error))`, `throw new Error(error instanceof Error ? error.message : "Failed to X")`, and `error: any`). `patterns.md` Handler Pattern is silent on error handling, and the try/catch wrappers effectively log-and-rethrow with no added value | `backend/src/graphql/handlers/auth/*.handler.ts`, `backend/src/graphql/handlers/events/*.handler.ts`, `backend/src/graphql/handlers/venues/*.handler.ts` (17 files), `docs/patterns.md:48-53` | Document a single convention in `patterns.md`: "Handlers do NOT try/catch — let Yoga surface the error; services throw `Error` with user-facing messages; auth failures go through `requireAuth()` with `extensions.code='UNAUTHENTICATED'`." Strip the boilerplate try/catch from all handlers | M |
| 🟡 | Cross-cutting | Urql operations are untyped across both features — Events CRUD screens use `any` pervasively (`create.tsx`, `edit.tsx`, `[id]/index.tsx`), and Auth screens access `result.data?.signUp` / `result.data?.signIn` which are implicitly `any` because no types are generated from the schema. This hid the Events CRUD `$id` vs `eventId` variable-name bugs (4 × 🔴) — with typed operations the type-check would have caught them at build time | `rn-app/lib/graphql/operations/*.ts`, `rn-app/app/(auth)/*.tsx`, `rn-app/app/(app)/events/**/*.tsx` | Adopt a GraphQL codegen pipeline (`@graphql-codegen/cli` with `typescript-urql` plugin) that produces typed hooks/operations; refactor both features' screens to consume generated types; document the pipeline in `patterns.md` Frontend Patterns + `tech.md` API Architecture | L |
| 🟡 | Cross-cutting | `patterns.md` silence is the proximate cause of drift in both features — the doc has no Error Handling, Transactions, Validation-failure shape, Export style, Auth-guard, or Database Patterns sections. Every drift captured in Auth + Events CRUD tables traces back to a missing section, so fixing per-feature rows without fixing the doc just re-introduces the drift on the next feature | `docs/patterns.md` | Run a single `/architect patterns` pass that adds all missing sections in one commit (Error Handling, Transactions, Validation, Exports, Auth Guard, Database Patterns, Component Extraction, Type Safety); reference this audit as the driver. All per-feature "document in patterns.md" recommendations collapse into this single effort | L |
| 🟢 | Cross-cutting | Service file naming drift: both features mix `<verb>.service.ts` (use-case) and `<noun>.service.ts` (utility, e.g., `jwt.service.ts`, `password.service.ts`) — Auth has this explicitly (🟢), Events CRUD also mixes single-method `run()` services with multi-method services (🟡 `GetEventsService`, `ManageTiersService`). The convention is under-specified across both features | `backend/src/domain/auth/common/*.service.ts`, `backend/src/domain/events/get-events.service.ts`, `backend/src/domain/events/manage-tiers.service.ts`, `docs/patterns.md:41-46` | In `patterns.md` Service Pattern, document the two sub-conventions: (a) `domain/<feature>/common/<noun>.service.ts` for utility helpers with named function exports; (b) `domain/<feature>/<verb>.service.ts` for use-case services — class with `run()` when single-use, methods may share a file only when they share the same aggregate + auth rules | S |

### Doc currency

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
| 🔴 | Doc currency | Contradicted: `design-system.md` documents a custom color palette (`primary` #1a237e, `primary-light` #00838f, `surface` #ffffff, `background` #1a1a2e, `text-primary`, `text-secondary`, `error` #dc2626, `success` #16a34a) but the Tailwind / NativeWind theme in `rn-app/tailwind.config.js` + `rn-app/global.css` is the stock shadcn palette (`--primary: 240 5.9% 10%`, `--secondary: 240 4.8% 95.9%`, etc.) — no `primary-light`, `surface`, `text-primary`, `text-secondary`, `error`, `success` exist as tokens. Components end up hardcoding hex values (`#64748b`, `#00838f`, `#1a237e`, `#dc2626` in `event-card.tsx:25-29`, `status-badge.tsx:18-27`) | `docs/design-system.md:17-38` and `rn-app/tailwind.config.js:10-44`, `rn-app/global.css:5-48` | Either (a) update the Tailwind theme to match design-system.md tokens (add CSS variables for `primary-light`, `surface`, `text-primary`, `text-secondary`, `error`, `success`), or (b) rewrite `design-system.md` Colors section to document the actual shadcn-based `hsl(var(--x))` token system. Option (a) is aligned with the "no AI slop, distinctive identity" direction | L |
| 🔴 | Doc currency | Contradicted: `design-system.md` specifies Space Grotesk as the font family, but no custom font is loaded in the rn-app — no `expo-font` usage, no font entry in `app.json`, no Google Fonts import. System font is in use. The "wide/extended sans-serif" design intent is nominal only | `docs/design-system.md:41` and `rn-app/app.json`, `rn-app/app/_layout.tsx` | Either load Space Grotesk via `expo-font` + `useFonts` in `app/_layout.tsx` and apply it via NativeWind utilities, or update `design-system.md` to acknowledge system font is used pending font integration | M |
| 🔴 | Doc currency | Contradicted: `patterns.md` Repository Pattern says "Exports a factory function or object with CRUD methods", but all six repositories (`user.repository.ts`, `event.repository.ts`, `venue.repository.ts`, `door-sale-tier.repository.ts`, `event-team-member.repository.ts`) are default-exported classes. Engineers following the doc literally will write factory functions that don't match any existing example | `docs/patterns.md:35-39` | Rewrite Repository Pattern to document "class-based Drizzle repositories implementing an interface; default export is the concrete class, named export is the interface" (or pick one approach and refactor all repositories). This overlaps with individual Auth/Events rows but the structural gap is the patterns doc itself | S |
| 🔴 | Doc currency | Contradicted: `patterns.md` Handler Pattern says "Calls the appropriate service", but `list-venues.handler.ts` and `get-venue.handler.ts` call `context.services.venueRepository` directly — there is no venue service in the domain layer. Anyone reading patterns.md for guidance will write one thing; reading existing code will copy the other | `docs/patterns.md:48-53` and `backend/src/graphql/handlers/venues/*.handler.ts` | Either add `list-venues.service.ts` / `get-venue.service.ts` to match the doc, or amend the doc to say "handlers MAY call repositories directly for trivial pass-through reads". Pick one and be explicit — current silence drives re-invention | S |
| 🔴 | Doc currency | Contradicted: `tech.md` Environment Variables table lists `JWT_SECRET`, but the actual env variable read by `jwt.service.ts` is `JWT_SECRET_STRING`. An engineer following `tech.md` will set the wrong variable name and see the fatal `process.exit(1)` at startup | `docs/tech.md:61` and `backend/src/domain/auth/common/jwt.service.ts:6-9` | Either rename the env var in code to `JWT_SECRET` to match the doc, or update the doc's Environment Variables table to say `JWT_SECRET_STRING` | S |
| 🔴 | Doc currency | Contradicted: `backlog.md` marks Events CRUD as `🔵 IN PROGRESS`, but all 33 acceptance criteria are implemented (with bugs captured separately in this audit). Status is misleading — readers looking at the backlog see Events CRUD as unfinished when it has shipped | `docs/backlog.md:16` | Change status to `🟢 DONE` (with a note referencing the audit for outstanding defects) or introduce a new `🟡 SHIPPED WITH KNOWN BUGS` status. Do not leave as IN PROGRESS — it understates delivered scope | S |
| 🟡 | Doc currency | Missing: `patterns.md` has no "Error handling" section — services throw plain `Error`, handlers wrap with varying try/catch shapes, GraphQL handlers bypass auth-error code conventions. Every new feature re-invents its own error conventions (already captured 3 distinct handler-catch shapes across Events CRUD) | `docs/patterns.md` (no section exists) | Add an "Error Handling" section specifying: (a) services throw `Error` with user-facing messages, (b) handlers do NOT try/catch (let Yoga surface the error), (c) auth failures throw `GraphQLError` with `extensions.code = 'UNAUTHENTICATED'` via a shared `requireAuth(context)` helper, (d) Zod `safeParse` + uniform error translation | M |
| 🟡 | Doc currency | Missing: `patterns.md` has no section on transactions. `create-event.service.ts` performs two sequential awaits against two repositories (event + event-team-member) without a transaction. The silence means every future multi-table write will re-litigate the question | `docs/patterns.md` (no section exists) | Add a "Transactions" section: "Services that write to more than one table MUST wrap writes in `db.transaction(async (tx) => {...})`; repositories accept an optional `tx` parameter defaulting to `this.db`". Overlaps with 🔴 BR-1 row in Events CRUD — this is the doc-level gap | M |
| 🟡 | Doc currency | Missing: `patterns.md` has no section on validation-failure shape. Three distinct `ZodError` handling patterns exist across Events CRUD services (`schema.parse()` raw throw, `safeParse` + concatenation, `safeParse` + hardcoded message). Doc says "Validates input with Zod schemas" but prescribes no failure shape | `docs/patterns.md:42-46` (Service Pattern) | Extend Service Pattern: "Use `safeParse`; on failure, throw a single `Error` whose message matches the corresponding spec error-handling row". Document the canonical shape and apply to existing services | S |
| 🟡 | Doc currency | Missing: `patterns.md` has no section on export style (default vs named). Repositories default-export classes + named-export interfaces; services default-export classes; GraphQL ops are named exports; types are named exports; frontend components are named exports. Inconsistent and undocumented — import ergonomics drift with each new file | `docs/patterns.md` (no section exists) | Add a "Module Exports" section: pick one style (e.g., named exports everywhere; default export reserved for React components if at all) and document. Also document `~/` alias in rn-app | S |
| 🟡 | Doc currency | Missing: `patterns.md` has no section on test conventions. `backend/package.json` has Jest + ts-jest in devDependencies but there are no tests in the repo, no documented test-file location, no documented test naming | `docs/patterns.md` (no section exists) and `backend/package.json:40-41` | Either add a "Testing" section (test-file colocation or `__tests__/`, naming, what to test), or remove Jest if testing is deferred. Same question for rn-app | M |
| 🟡 | Doc currency | Stale: `patterns.md` Service Pattern says "One file per use case" but `GetEventsService` (getById + listByCreator) and `ManageTiersService` (addTier + updateTier + removeTier) each expose multiple methods. Doc silence allows this, but new readers will wonder whether to split or group | `docs/patterns.md:41-46` | Clarify: "A service file may expose multiple methods when they share the same aggregate and authorization rules; otherwise one method named `run()`" — or split the multi-method services into per-use-case files | S |
| 🟡 | Doc currency | Missing: `patterns.md` does not document the `common/` directory convention (utility helpers inside a feature, e.g. `domain/auth/common/{jwt,password}.service.ts`). The `<noun>.service.ts` naming of utilities also contradicts the documented `<verb>.service.ts` convention | `docs/patterns.md:41-46` and `backend/src/domain/auth/common/` | Document: "Each `domain/<feature>/common/` folder holds utility helpers shared by that feature's services. Utility helpers use `<noun>.service.ts` naming; use-case services use `<verb>.service.ts`" | S |
| 🟡 | Doc currency | Missing: `patterns.md` has no Database Patterns / schema conventions section. Drizzle conventions (snake_case DB names with camelCase TS props, `varchar(24)` PK with `createId()`, `createdAt/updatedAt/deletedAt` timestamps, enum placement at top of schema.ts, `mapToEntity` helper per repository, entity type files with `.entity.ts` suffix exposing both `XSchema` and `XEntity`) are consistent in code but entirely absent from the doc | `docs/patterns.md` (no section exists) and `backend/src/db/schema.ts`, `backend/src/repositories/*.entity.ts` | Add a "Database Patterns" section covering: column naming, id/primary-key shape, default/not-null defaults, enum placement, `mapToEntity`, entity file split (`XSchema` vs `XEntity`), soft-delete columns | M |
| 🟡 | Doc currency | Missing: `patterns.md` has no auth-guard convention for handlers. 14 handlers re-implement `if (!context.user) throw new Error("Authentication required")`; none set `extensions.code = 'UNAUTHENTICATED'` (root cause of urql's `didAuthError` never firing — see 🔴 Auth Edge Case 3) | `docs/patterns.md:48-53` and `backend/src/graphql/handlers/**/*.handler.ts` | Document a `requireAuth(context)` helper convention: "handlers calling `requireAuth(context)` get a typed user and consistent `GraphQLError` with `extensions.code='UNAUTHENTICATED'` on failure"; add the helper in `graphql/handlers/common/` | S |
| 🟡 | Doc currency | Missing: `patterns.md` Resolver Wiring says "Keep flat — no nested resolver objects unless needed for type resolution" but doesn't prescribe where type resolvers live when they grow, and gives no guidance on letting type resolvers call repositories. Today `Event.createdBy` resolver in `resolvers/index.ts:53-60` constructs a raw Drizzle query, violating the data-access-layer rule | `docs/patterns.md:55-57` | Clarify: "Type resolvers for entity X live in `graphql/resolvers/<entity>.resolver.ts` and are re-exported from `resolvers/index.ts`. Type resolvers MUST go through repositories — no raw Drizzle queries" | S |
| 🟡 | Doc currency | Missing: `patterns.md` has no frontend typing strictness rules. `create.tsx`, `[id]/edit.tsx`, `[id]/index.tsx` use `any` pervasively for urql results and handler arg types (already captured at file level in Events CRUD). Doc silence lets this grow indefinitely | `docs/patterns.md:59-81` (Frontend Patterns) | Add a "Type Safety" rule: "no `any` in `.ts`/`.tsx` screens; generate types from the GraphQL schema (e.g., `@graphql-codegen/cli` or `graphql-request`) and consume them from urql operations" | S |
| 🟡 | Doc currency | Missing: `patterns.md` has no component-extraction guidance. Screens regularly exceed 500 LOC (`create.tsx` 475, `edit.tsx` 564) with modals duplicated across files. Doc provides no LOC threshold or rule on where shared modals/forms live | `docs/patterns.md:59-81` | Add: "Screens stay under ~300 LOC; shared modals/forms/bottomsheets live in `rn-app/components/`. Duplicated JSX across two or more screens must be extracted before merging" | S |
| 🟡 | Doc currency | Missing: `patterns.md` Project Structure diagram does not include `backend/src/graphql/graphql.types.ts`, `backend/src/db/index.ts`, `rn-app/lib/graphql/{client,provider,operations/}`, `rn-app/hooks/`, or `rn-app/components/ui/` (mentioned but not listed). Anyone trying to orient themselves from the diagram misses several core files | `docs/patterns.md:7-31` | Update the diagram to include all actual top-level files/dirs; note Entity + Repository file pairing | S |
| 🟡 | Doc currency | Missing: `tech.md` mentions `urql 4.2.2` as a dependency but omits the client architecture entirely — no mention of `authExchange`, `cacheExchange`, `fetchExchange`, the `GraphQLProvider` in `lib/graphql/provider.tsx`, operation files in `lib/graphql/operations/`, or the `EXPO_PUBLIC_GRAPHQL_URL` env var that configures the client | `docs/tech.md:17-24` and `rn-app/lib/graphql/client.ts`, `provider.tsx` | Add a "GraphQL Client" subsection under API Architecture covering exchange order, auth-exchange behaviour, provider setup, operation file layout, and the env var | M |
| 🟡 | Doc currency | Missing: `tech.md` does not describe the Zustand store architecture beyond listing the version — no mention of `rn-app/stores/` location, SecureStore integration, web `localStorage` fallback, the `restoreToken` / `useRestoreSession` flow, or how the store wires into urql's `addAuthToOperation` | `docs/tech.md:20` and `rn-app/stores/auth.store.ts`, `rn-app/hooks/useRestoreSession.ts` | Add a "State Management" subsection covering store layout, persistence (SecureStore + localStorage fallback), and restore-session flow | M |
| 🟡 | Doc currency | Missing: `tech.md` does not describe the Expo Router structure — no mention of `(auth)` / `(app)` groups, `useAuthGate` hook, or the `_layout.tsx` routing pattern | `docs/tech.md:18` and `rn-app/app/` | Add a "Routing" subsection: group conventions, auth gating, shared layouts | S |
| 🟡 | Doc currency | Missing: `tech.md` does not list `@paralleldrive/cuid2` (used in `backend/src/db/schema.ts:2` for primary-key generation). It's installed at the workspace root (`package.json`), not in `backend/package.json` — doubly undocumented and unconventional for a monorepo | `docs/tech.md:6-23` (stack table) and root `package.json` | Add CUID2 to the stack table; also consider moving the dependency to `backend/package.json` where it's actually used | S |
| 🟡 | Doc currency | Stale: `tech.md` says "Push schema changes: `npx drizzle-kit push`" but `backend/package.json` defines `db:generate`, `db:migrate`, `db:studio` scripts. The workflow is actually `pnpm db:generate` → `pnpm db:migrate`. Doc describes a different path | `docs/tech.md:54` and `backend/package.json:10-13` | Update Database Management section to document the actual script workflow (`pnpm db:generate` to create migration, `pnpm db:migrate` to apply, `pnpm db:studio` to inspect) | S |
| 🟡 | Doc currency | Missing: `tech.md` does not document authentication middleware — `backend/src/index.ts:72-84` extracts the Bearer token in Yoga's `context` callback and sets `context.user`. The "Authentication Flow" section describes this behaviour abstractly but not the implementation location | `docs/tech.md:71-75` and `backend/src/index.ts:72-84` | Reference the specific file/lines so engineers can find the entry point when adding new auth behaviour | S |
| 🟡 | Doc currency | Stale: `design-system.md` Component Inventory lists 10 components as "⬚ Planned" that are actually built in `rn-app/components/ui/`: `EventCard` (`event-card.tsx`), `StatusBadge` (`status-badge.tsx`), `SectionHeader` (`section-header.tsx`), `TierRow` (`tier-row.tsx`), `ConfirmDialog` (`confirm-dialog.tsx`), `SkeletonLoader` (`skeleton-loader.tsx`), `EmptyState` (`empty-state.tsx`), `FloatingActionButton` (`floating-action-button.tsx`), `ToggleGroup` (`toggle-group.tsx`) | `docs/design-system.md:86-98` and `rn-app/components/ui/` | Mark each as ✅ Built with the actual file path; keep `BottomSheet`, `DateTimePicker`, `VenuePickerSheet`, `CreateVenueSheet` as Planned (still not extracted) | S |
| 🟡 | Doc currency | Missing: `design-system.md` does not describe the theme-toggle mechanism. `rn-app/components/ThemeToggle.tsx` exists; `lib/useColorScheme.tsx` exists; dark-mode switching is wired through Tailwind's `dark:` prefix and `darkMode: 'class'`. None of this is in the design-system doc | `docs/design-system.md` (no section) and `rn-app/components/ThemeToggle.tsx`, `rn-app/tailwind.config.js:5` | Add a "Theme Modes" section documenting dark-first posture, how theme toggles, and interaction with the CSS-variable tokens | S |
| 🟡 | Doc currency | Missing: `business-rules.md` Door Sales Configuration section is missing the tier-validation rules implied by spec AC-32 ("Adding a tier with empty name or non-positive price returns validation error"). BR-DSC-002 documents the shape of valid tiers, but the doc does not state the validation outcome for invalid input. Same gap for non-Manager tier mutations (spec AC-31) | `docs/business-rules.md:67-70` | Add: `BR-DSC-004` "Tier with empty name or non-positive price is rejected with 'Please provide a tier name and a price greater than zero'"; `BR-DSC-005` "Only the event's Manager can add, update, or remove tiers" (or unify with BR-DSC-001) | S |
| 🟡 | Doc currency | Missing: `business-rules.md` Events section does not carry spec AC-17 "Non-manager user attempting to edit/delete/transition returns authorization error" as an explicit rule. BR-EVT-008 documents positive Manager permission but not the rejection behaviour / error message | `docs/business-rules.md:41-49` | Add: `BR-EVT-010` "Non-manager user attempting to edit, delete, or transition an event receives 'You do not have permission to edit this event'" | S |
| 🟡 | Doc currency | Stale: `business-rules.md` Auth section has 6 rules but `features/auth/spec.md` has 5. `BR-AUTH-006` "Expired or invalid token on restore silently redirects to sign-in" comes from the spec's Error Handling table, not its Business Rules. This conflates rules with error-handling — either the doc or the spec is the consolidator; currently they disagree on scope | `docs/business-rules.md:14` and `docs/features/auth/spec.md:11-16` | Decide: does `business-rules.md` include error-handling derived rules? If yes, audit all specs for similar rows and add them; if no, remove BR-AUTH-006 | S |
| 🟡 | Doc currency | Stale: `business-rules.md` contains rules for unimplemented / unspecced features (Lists, Guests, Check-in, Door Sales Recording, Notifications, Analytics). These were lifted from the MVP design doc but have no backing spec, so they are implementation-suggestive rather than product-approved. Readers cannot tell which rules are enforced vs aspirational | `docs/business-rules.md:72-122` | Either (a) move "not yet implemented" sections to a separate `docs/future-business-rules.md` / draft file, or (b) add a clear banner to those sections ("These rules are provisional and derived from the MVP design. They become binding only once the corresponding feature spec is approved.") | S |
| 🟢 | Doc currency | Missing: `tech.md` does not list `react-native-reanimated`, `lucide-react-native`, `@rn-primitives/*`, `class-variance-authority`, `tailwind-merge`, `tailwindcss-animate` — all used in the rn-app for styling/animation primitives | `docs/tech.md:6-23` | Add a "Frontend Libraries" row group for the animation/icon/primitives layer | S |
| 🟢 | Doc currency | Missing: `patterns.md` has no Frontend — Component Patterns section. Today `components/ui/` contains both primitives (button, input, card, text) and compound components (event-card, status-badge, tier-row). Doc says nothing about the split | `docs/patterns.md:59-81` | Add a Component Patterns section: primitives under `components/ui/primitives/` (or keep flat), feature components under `components/<feature>/`, shared primitives under `components/ui/` | S |
| 🟢 | Doc currency | Missing: `patterns.md` Naming Conventions does not include Entity files (`<entity>.entity.ts`) or Repository interfaces/implementations (`Drizzle<DB><Entity>Repository`) — both established conventions in `backend/src/repositories/` | `docs/patterns.md:83-89` | Add: `<entity>.entity.ts` for entity type files; `Drizzle<DB><Entity>Repository` as the concrete class naming convention | S |
| 🟢 | Doc currency | Contradicted: `design-system.md` Status Colors table (`status-draft`/`status-active`/`status-finished`/`status-cancelled`) references token paths like `primary-light` (#00838f) and `primary` at 60%, but components hardcode these. Not a separate finding from the 🔴 color-tokens issue above, but worth noting: status colors doubly drift — token ref is broken AND the hex opacity is applied inline (`bg-[#1a237e]/60` in `status-badge.tsx:23`) | `docs/design-system.md:33-37` and `rn-app/components/ui/status-badge.tsx:12-28` | Resolve as part of the color-tokens fix above; ensure status colors use the same tokens after migration | S |
| 🟢 | Doc currency | Missing: `design-system.md` does not reference `rn-app/components/ThemeToggle.tsx` or `rn-app/components/layout/` (an existing subdirectory). These may be out of scope for the design system but at minimum the layout/ directory's existence should be acknowledged or pruned | `docs/design-system.md:76-100` and `rn-app/components/` | Audit whether these components are still used; either inventory them or remove the files | S |
| 🟢 | Doc currency | Missing: `tech.md` "API Architecture > Context Shape" example shows `services: { /* all service instances */ }` but the actual context also has `venueRepository` and `doorSaleTierRepository` mixed in (see Events CRUD 🟡 row about context namespace). Doc oversimplifies | `docs/tech.md:78-85` and `backend/src/graphql/graphql.types.ts:17-30` | After the Events CRUD context-split refactor, update the doc to reflect the actual shape | S |
| 🟢 | Doc currency | Missing: `backlog.md` does not reference where "spec status" definitions live or what promotes a feature from IDEA → TODO → IN PROGRESS → DONE. Status legend lists the symbols but not the gating rules | `docs/backlog.md:3-10` | Add a "Status transitions" subsection describing promotion rules (e.g., "IDEA → TODO requires approved spec; TODO → IN PROGRESS requires approved plan; IN PROGRESS → DONE requires validation report") | S |

### Agent skills

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
| 🟡 | Agent skills | Stale: `po/SKILL.md` says the PO "updates `docs/business-rules.md`" when writing/changing specs, but business-rules.md currently includes rules for 6 unspecced features (Lists, Guests, Check-in, Door Sales Recording, Notifications, Analytics) — lifted from the MVP design doc, not from any spec. The workflow promises spec → rules traceability but the doc has content with no specs | `.claude/skills/po/SKILL.md:14` and `docs/business-rules.md:72-122` | Tighten the command: "`/po` may only add rules to business-rules.md that have a corresponding approved spec; rules sourced from the MVP doc without a spec belong in `docs/future-business-rules.md`". Or accept the drift and document it | S |
| 🟡 | Agent skills | Missing: All three skill files (`po`, `architect`, `designer`) predate this audit and have no mention of `docs/audits/` — the directory does not appear in any skill's "Your Documents" table, and no skill captures the workflow of creating audits or integrating audit findings into backlog/patterns/design updates | `.claude/skills/po/SKILL.md:11-18`, `architect/SKILL.md:11-18`, `designer/SKILL.md:11-17` | Decide which skill owns audits (or create a new `/auditor` skill); add `docs/audits/` to the relevant skill's document table with a command like `/po audit-validate` or `/architect audit-review` | M |
| 🟡 | Agent skills | Stale: `designer/SKILL.md` Typography guidance says "The MVP doc calls for wide/extended sans-serif (Space Grotesk, Outfit) — commit to these or propose better alternatives" but no custom font is loaded in the rn-app (system font is used). The skill treats Space Grotesk as committed while the codebase has not integrated it. The `/designer review` command has no criterion to flag missing font integration | `.claude/skills/designer/SKILL.md:104-105` and `rn-app/app.json`, `rn-app/app/_layout.tsx` | Either (a) add a step to `/designer system` to verify font integration and flag the gap, or (b) acknowledge in the skill that Space Grotesk is aspirational until loaded | S |
| 🟡 | Agent skills | Stale: `designer/SKILL.md` `/designer review` command lists the review criteria as "wrong color, spacing, etc." but has no criterion for "hardcoded hex values instead of theme tokens". Given components universally hardcode colors (see Doc currency 🔴 color-tokens row), the review command as written cannot catch the single biggest design-system drift | `.claude/skills/designer/SKILL.md:35-42` | Add an explicit review criterion: "Fail if color/spacing/typography values are hardcoded instead of referencing theme tokens (`hsl(var(--x))` / design-system.md tokens)" | S |
| 🟡 | Agent skills | Stale: `designer/SKILL.md` `/designer system` step 3 says "Check NativeWind/Tailwind config for current theme values" but design-system.md documents a token set (`primary-light`, `surface`, `text-primary`, etc.) that does not exist in `rn-app/tailwind.config.js`. The skill's workflow would surface this drift every time it ran — either it hasn't been run, or the step's reconciliation logic isn't explicit enough | `.claude/skills/designer/SKILL.md:48` | Expand step 3: "Diff the tokens documented in `design-system.md` against those defined in `tailwind.config.js` + `global.css`; fail the update if they diverge until reconciled" | S |
| 🟡 | Agent skills | Missing: `architect/SKILL.md` `/architect` step 4 says "Explore the codebase to understand existing patterns in the relevant area" but the Rules section assumes `patterns.md` is the source of truth ("Follow existing patterns — do not introduce new architectural patterns without documenting them"). When patterns.md is sparse (as it is today), the architect has no documented way to resolve conflicts between code and doc | `.claude/skills/architect/SKILL.md:28,96-98` | Add a rule: "If code and `patterns.md` disagree, use `/architect patterns` to reconcile BEFORE writing the plan. The plan may not cite patterns that contradict the doc without first updating the doc" | S |
| 🟡 | Agent skills | Missing: `architect/SKILL.md` plan template has no "Testing" section — but `backend/package.json` has Jest installed and patterns.md is silent on tests (separately flagged in Doc currency). The skill prescribes implementation steps but not test steps | `.claude/skills/architect/SKILL.md:55-91` | Add a "Testing" section to the plan template (e.g., "List the test files to create, what they cover, and what the test strategy is for this feature"). Blocks on the broader decision about whether tests are expected | S |
| 🟡 | Agent skills | Missing: `architect/SKILL.md` "Data Model Changes" step says "Schema additions/modifications with exact Drizzle column definitions" but does not mention the migration workflow (`pnpm db:generate` → `pnpm db:migrate`). When a plan ships schema changes, the current workflow must be documented or engineers will hand-apply SQL | `.claude/skills/architect/SKILL.md:62-63` | Add a sub-step: "After schema changes, run `pnpm db:generate` to produce the migration; ship the migration file in the feature branch" | S |
| 🟢 | Agent skills | Missing: `po/SKILL.md` does not describe how to handle inherited MVP-design-doc rules (like the ones in `business-rules.md:72-122`). The workflow is spec-first but the aggregated doc has spec-less rules. The skill should either allow "seeding rules from MVP doc" explicitly or forbid them | `.claude/skills/po/SKILL.md:83-91` | Add a Rule: "Rules in business-rules.md without a backing spec must carry a `(source: MVP design doc)` annotation and be marked provisional" | S |
| 🟢 | Agent skills | Missing: Document-ownership table in `po/SKILL.md` doesn't mention `docs/features/<feature>/spec.md` error-handling + edge-cases consistency with `business-rules.md`. Auth BR-AUTH-006 proves the disconnect (spec has 5 BRs, consolidated doc has 6) | `.claude/skills/po/SKILL.md:11-17` | Add rule: "When updating business-rules.md, cite the source spec section (Business Rules, Error Handling, or Edge Cases) that each new rule comes from" | S |
