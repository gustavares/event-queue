# Full Project Audit — 2026-04-16

**Status:** In progress
**Design:** [docs/superpowers/specs/2026-04-16-full-audit-design.md](../superpowers/specs/2026-04-16-full-audit-design.md)
**Plan:** [docs/superpowers/plans/2026-04-16-full-audit.md](../superpowers/plans/2026-04-16-full-audit.md)

## Executive Summary

_Populated in Task 8 after all findings are collected._

- 🔴 Critical: —
- 🟡 Important: —
- 🟢 Nice-to-have: —

### Top takeaways

_Populated in Task 8._

## Findings

### Auth

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
| 🔴 | Auth | Edge case 3: Token-expires-mid-session flow is broken — `didAuthError` in the urql auth exchange checks for `extensions.code === 'UNAUTHENTICATED'` but the backend never emits that code; expired tokens silently result in `user = null` in context, so protected resolvers return null/empty data instead of triggering a session clear and redirect | `rn-app/lib/graphql/client.ts:21` and `backend/src/index.ts:81` | Have protected resolvers (e.g. `myEvents`, `me`) throw a GraphQL error with `extensions: { code: 'UNAUTHENTICATED' }` when `context.user` is null, so the urql `didAuthError` check actually fires and triggers `clearAuth()` | M |
| 🟡 | Auth | Error 1: Duplicate-email error message wording diverges from spec — spec requires `"An account with this email already exists"`, but `signup.service.ts` throws `"${email} already registered"`, which surfaces verbatim in the Alert | `backend/src/domain/auth/signup.service.ts:43` | Change the thrown message to `"An account with this email already exists"` | S |
| 🟡 | Auth | Edge case 2: Client-side empty-form guard is a truthy check, not a proper validation — `!email` passes whitespace-only strings through to the server; no format or minimum-length validation exists on the client before submission | `rn-app/app/(auth)/sign-in.tsx:18` and `rn-app/app/(auth)/sign-up.tsx:19` | Trim input values and add basic client-side checks (valid email format, password min length) before calling the mutation, matching the server-side Zod schema constraints | S |
| 🟡 | Auth | Edge case 4: Network error message shown raw — urql's `CombinedError.message` for network failures is a technical string (e.g. `"[Network] Failed to fetch"`); shown as-is in an `Alert`, which is not user-friendly and diverges from the spec intent of a "network error message" | `rn-app/app/(auth)/sign-in.tsx:25` | Inspect `result.error.networkError` separately and display a human-readable message such as `"Network error — please check your connection and try again"` | S |
| 🟡 | Auth | Pattern: Repository pattern — concrete class exported as `default`, interface as named export; patterns.md documents "factory function or object with CRUD methods" but implementation uses a class; the default/named split also makes import ergonomics inconsistent with every other module in the codebase | `backend/src/repositories/user.repository.ts:17` | Either align with the documented "factory function" shape, or update patterns.md to say "class or factory function"; also consolidate to named exports | S |
| 🟡 | Auth | Pattern: Module-level side effect — `dotenv.config()` runs at import time in `jwt.service.ts`, which means any module that imports this file triggers environment loading as a hidden side effect; also calls `process.exit(1)` at module scope if `JWT_SECRET_STRING` is missing, making the module untestable in isolation | `backend/src/domain/auth/common/jwt.service.ts:5` | Refactor to accept a config/secret argument in the exported functions, or initialise via an explicit `init()` call driven by the app entry point; document the convention in patterns.md under Backend Patterns | M |
| 🟢 | Auth | New pattern: Utility services named as nouns (`password.service.ts`, `jwt.service.ts`) — patterns.md specifies `<action>.service.ts` (verb) for use-case services; these are shared helper utilities living in `common/`, not use-case services, so the naming convention doesn't map cleanly | `backend/src/domain/auth/common/password.service.ts` and `backend/src/domain/auth/common/jwt.service.ts` | Document in patterns.md: files in `domain/<feature>/common/` are utility helpers and follow `<noun>.service.ts` naming to distinguish them from use-case services | S |
| 🟢 | Auth | New pattern: Store-level storage helpers — `auth.store.ts` defines `saveToken`, `getToken`, `deleteToken` as module-level functions co-located with the Zustand store; patterns.md Store Pattern says nothing about where persistence helpers live | `rn-app/stores/auth.store.ts:23-45` | Document in patterns.md that storage helpers may be co-located in the store file when they are used only by that store; consider flagging fire-and-forget `saveToken(token).catch(console.error)` at line 54 — a SecureStore failure silently diverges persisted vs in-memory state | S |
| 🟢 | Auth | New pattern: Entity type file co-located with repository — `user.entity.ts` sits alongside `user.repository.ts` in `repositories/`; patterns.md documents only "one file per entity" but doesn't describe entity type files or their `.entity.ts` suffix convention | `backend/src/repositories/user.entity.ts` | Document in patterns.md: entity type files use the `<entity>.entity.ts` suffix and live in `repositories/` alongside their repository file | S |
| 🟢 | Auth | New pattern: Error handling — services uniformly use `throw new Error(...)` and handlers re-throw with the same message; no error type hierarchy, no Result/Either types, no documented convention; patterns.md is silent on error handling strategy | `backend/src/domain/auth/signup.service.ts:35,42` and `backend/src/graphql/handlers/auth/signup.handler.ts:31` | Document the chosen convention in patterns.md ("services throw plain `Error`; handlers catch and re-throw to preserve the message to the GraphQL layer"); existing TODO comments in signup.service.ts also flag desire for custom domain errors — decide and document | S |

### Events CRUD

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
| 🔴 | Events CRUD | AC-12: "Reopen Event" is broken end-to-end — the reopen handler calls `handleTransition('DRAFT')`, but the spec + server allow only `FINISHED → ACTIVE`; the server correctly rejects this as "This event cannot be changed to that status", so the user can never reopen a finished event | `rn-app/app/(app)/events/[id]/index.tsx:82` | Change the `reopen` branch to `handleTransition('ACTIVE')` (and update the confirmation dialog copy "The event will be moved back to draft status" on line 119 to say "active" to match) | S |
| 🔴 | Events CRUD | AC-10/11/12/13: `transitionStatus` is invoked with the wrong variable name — the mutation expects `$id: ID!` but the client passes `{ eventId: id, status: ... }`; urql sends the request with an unused variable and a missing one, so every Publish / Close / Cancel / Reopen action fails with a GraphQL variable error | `rn-app/app/(app)/events/[id]/index.tsx:67` | Pass `{ id, status: toStatus }` to match the mutation definition at `rn-app/lib/graphql/operations/events.ts:99` | S |
| 🔴 | Events CRUD | AC-15: `deleteEvent` is invoked with the wrong variable name — mutation expects `$id: ID!` but client passes `{ eventId: id }`, so soft-delete never happens and the subsequent `router.replace('/(app)')` leaves the event present on the list | `rn-app/app/(app)/events/[id]/index.tsx:73` | Pass `{ id }` to match the mutation definition | S |
| 🔴 | Events CRUD | AC-29/30: `updateDoorSaleTier` and `removeDoorSaleTier` are invoked with wrong variable name — mutations expect `$id: ID!` but the edit screen passes `{ tierId: ... }`; GraphQL server rejects with a "Variable not defined" error, so tier updates and removals never reach the backend | `rn-app/app/(app)/events/[id]/edit.tsx:206`, `:213-215`, `:228` | Pass `{ id: tier.id, input: ... }` and `{ id: tier.id }` respectively; align all four mutation calls with their operation definitions | S |
| 🔴 | Events CRUD | BR-14 / Edge 7: Disabling door sales deletes all existing tiers instead of preserving them — the edit screen iterates `eventData.event.doorSaleTiers` and removes each via the tier mutation when `doorSalesEnabled === false`, directly contradicting "Disabling door sales does not delete existing tier configuration (preserved for reactivation)" | `rn-app/app/(app)/events/[id]/edit.tsx:226-229` | Remove the delete-all branch; tiers should only be mutated when `doorSalesEnabled === true`. Persisting the `doorSalesEnabled=false` flag alone is sufficient to mark door sales inactive | S |
| 🔴 | Events CRUD | BR-1: Event + EventTeamMember creation is NOT wrapped in a DB transaction — `CreateEventService.run` issues two separate awaits (event insert, then team-member insert); a DB error between the two calls (connection drop, unique constraint on `event_team_member`, validation race) leaves an orphaned event with no Manager, which breaks every downstream authorization check and makes the event unreachable via `myEvents`/`event` | `backend/src/domain/events/create-event.service.ts:58-74` | Wrap both inserts in a single Drizzle `db.transaction(async (tx) => { ... })`; pass `tx` into both repository calls (repositories may need an overload that accepts an external tx) | M |
| 🟡 | Events CRUD | AC-9 / BR-2-4: Editing an event to switch from venue → custom location sends `venueId: null`, but the zod schema in `UpdateEventService` is `z.string().optional()` which rejects `null` (only `undefined` passes); `schema.parse()` throws "Expected string, received null", so the whole save fails with no user-visible reason | `rn-app/app/(app)/events/[id]/edit.tsx:181` and `backend/src/domain/events/update-event.service.ts:27` | Either (a) change the client to send `venueId: undefined` when switching to custom, or (b) relax the schema to `z.string().nullable().optional()` and update the type; option (b) is preferred since the entity allows null | S |
| 🟡 | Events CRUD | AC-26/27 / BR-12: The edit screen never sends `doorSalesEnabled` in the `updateEvent` input — changing the switch in the UI changes local state but the field is omitted from the mutation payload, so toggling off/on after an initial value is set silently does nothing on the server | `rn-app/app/(app)/events/[id]/edit.tsx:171-176` | Include `doorSalesEnabled` in the `input` object passed to `updateEvent` | S |
| 🟡 | Events CRUD | AC-19: `GetEventsService.getById` admits any team member (checks only that `membership` exists), but spec AC-19 says "User can fetch a single event by id if they are its Manager"; today the creator is the only team member so behaviour is equivalent, but the permission check is looser than the spec and will silently admit promoters/hosts when Team Management lands | `backend/src/domain/events/get-events.service.ts:17-20` | Either tighten to `membership.role !== 'MANAGER'` to match the spec literally, or update the spec to allow any team member (the latter likely reflects actual product intent — flag for PO review) | S |
| 🟡 | Events CRUD | Edge 4 / Error row "Invalid status transition": Reopening a CANCELLED event returns the generic "This event cannot be changed to that status"; spec edge case 4 requires the specific message "Cancelled events cannot be reopened." | `backend/src/domain/events/transition-event.service.ts:40` | Add a dedicated branch before the generic guard: `if (event.status === 'CANCELLED') throw new Error("Cancelled events cannot be reopened.")` | S |
| 🟡 | Events CRUD | Error row "Missing required event field": Spec mandates a single rolled-up message "Please fill in all required fields", but zod's default refinements throw per-field messages like `"name: String must contain at least 1 character(s)"` which surface verbatim to the client | `backend/src/domain/events/create-event.service.ts:18-42` | Catch ZodError in the service (or handler) and translate to the spec's consolidated message; alternatively, add custom messages on each `.min(1, "...")` so the client can still show inline errors, and rely on client-side validation for the rolled-up copy | S |
| 🟡 | Events CRUD | Error row "Venue missing name or address": Spec expects "Venue name and address are required", but `CreateVenueService` throws "Input validation failed: name: Name is required; address: Address is required" by concatenating zod errors — message diverges and is not user-friendly | `backend/src/domain/venues/create-venue.service.ts:27-29` | Translate zod validation failures to the spec message; the current `CreateVenueSchema` already uses `"Name is required"` / `"Address is required"` — just throw a single pre-composed string matching the spec | S |
| 🟡 | Events CRUD | Error row "Event not found": Spec says "Event not found" is accompanied by "Redirect to events list"; the current detail screen renders the error inline with a manual "Go back" link instead of auto-redirecting to `/(app)` | `rn-app/app/(app)/events/[id]/index.tsx:136-145` | On `!event && !fetching`, call `router.replace('/(app)')` (with an optional toast/banner explaining the redirect) instead of rendering the inline dead-end | S |
| 🟡 | Events CRUD | AC-6/7/8: Server-side validation errors ("Please select a venue or provide a location", "End time must be after start time", "Start time cannot be in the past") never reach the user on the Create screen — `handleSubmit` checks `result.error`, sets `submitting=false`, and returns silently; the form sits with the button re-enabled and no feedback | `rn-app/app/(app)/events/create.tsx:131-134` and `rn-app/app/(app)/events/[id]/edit.tsx:187-190` | Surface `result.error.graphQLErrors[0].message` (or a translated/mapped version) via `setErrors({ submit: ... })` or a toast; the current silent failure violates the "stay on form" + "highlight missing fields" behaviour spec | S |
| 🟡 | Events CRUD | AC-6 / BR-4 (client-side gap): The Create form's client validator requires `locationName` in custom mode but not `locationAddress`, so users can submit with an empty address; the server catches this via the venueId-or-(name+address) refine, but per the spec both inline fields are required — the client should prevent the trip to the server | `rn-app/app/(app)/events/create.tsx:80-88` and `:[id]/edit.tsx:135-143` | Add a parallel `!locationAddress.trim()` check for custom mode; show an inline error on the address field to match the spec's "Please select a venue or provide a location" intent | S |
| 🟡 | Events CRUD | Pattern consistency: Door-sale-tier mutations rely on `updateEvent` for the `doorSalesEnabled` flag rather than a dedicated `enableDoorSales`/`disableDoorSales` mutation as the spec's user stories read; combined with the edit-screen bugs above this makes the door-sales flow fragile. Documenting this explicitly in the spec (or adding the two mutations) would stop the pattern being re-invented on each client | `backend/src/graphql/schema/index.ts:130-145`, `docs/features/events-crud/spec.md:84-85` | Either (a) add `enableDoorSales(eventId: ID!)` / `disableDoorSales(eventId: ID!)` mutations, or (b) amend the spec to note "Enable/disable door sales is done via the generic `updateEvent` mutation" | S |
| 🟢 | Events CRUD | AC-32 (client-side): Create / edit screens let users add tiers with empty name or empty price fields and silently skip them on submit (`filter((t) => t.name.trim() && t.price.trim())`) rather than showing a validation error, which contradicts the spec's "Please provide a tier name and a price greater than zero" inline-error expectation | `rn-app/app/(app)/events/create.tsx:139` and `:[id]/edit.tsx:211` | Validate tier rows before submit; either highlight incomplete rows or block the submit with an inline error. Also guard against `parseFloat` yielding NaN for non-numeric price input | S |
| 🟢 | Events CRUD | BR-12 (door-sales enable/disable path): `manage-tiers.service.ts` checks Manager role directly, but adding a tier when `doorSalesEnabled === false` is not blocked — a tier created while door sales is off will be persisted and surface whenever sales is re-enabled. Spec is ambiguous here (BR-14 says tiers persist across enable/disable cycles), but the current behaviour lets you configure tiers for disabled sales, which surfaces in the UI as rows with no "enabled" indicator | `backend/src/domain/events/manage-tiers.service.ts:22-42` | Either (a) accept as intentional and document, or (b) reject `addTier` when `event.doorSalesEnabled === false`; decide with PO | S |
| 🟢 | Events CRUD | AC-25: `getVenue` returns `null` when no venue matches — spec doesn't say it should 404, but handler swallows the nullability and exposes the raw null to clients with no error distinction; parallels `Event not found` handling | `backend/src/graphql/handlers/venues/get-venue.handler.ts:14-19` | Decide: either keep permissive null (spec allows it) or throw "Venue not found" for parity with Event handling. Low priority since no UI consumes this yet | S |
| 🟢 | Events CRUD | BR-1: `event_team_member` has a uniqueIndex on `(eventId, userId)` (db/schema.ts:55-57), but `EventTeamMemberRepository.create` has no conflict handling — not an issue for Events CRUD itself (one Manager per event), but foreshadows Team Management needing `onConflictDoNothing`/`onConflictDoUpdate` clauses | `backend/src/repositories/event-team-member.repository.ts:24-35` | Document in patterns.md (or future Team Management spec); no change needed for this feature | S |
| 🟢 | Events CRUD | Doc drift: `plan.md` references an `UpdateEventDbInput.status` field (line 185) that IS present in `event.entity.ts`, but update-event.service.ts never passes status through — status mutations go exclusively through `transition-event.service.ts`. The shared Update input works fine, but mixing status into the generic `updateEvent` handler would bypass the transition rules; current code relies on discipline, not type-level guarantees | `backend/src/domain/events/update-event.service.ts:67-76` | Omit `status` from `UpdateEventDbInput` (create a dedicated `TransitionEventDbInput = { status: EventStatus }`) to make the separation enforceable at compile time | S |
| 🔴 | Events CRUD | Pattern: Repository pattern — every new repository (`event`, `venue`, `door-sale-tier`, `event-team-member`) uses `export default class Drizzle...Repository implements XRepository`, repeating the drift already noted in Auth; patterns.md documents "factory function or object with CRUD methods", so the whole data-access layer now diverges from the documented convention | `backend/src/repositories/event.repository.ts:34`, `venue.repository.ts:24`, `door-sale-tier.repository.ts:24`, `event-team-member.repository.ts:21` | Either align all repositories with the factory-function shape, or update patterns.md to document "class-based Drizzle repositories implementing an interface"; consolidate default/named export split in the same pass | M |
| 🔴 | Events CRUD | Pattern: Handler pattern — `list-venues.handler.ts` and `get-venue.handler.ts` bypass the service layer and call `context.services.venueRepository` directly; patterns.md says "Handler: Calls the appropriate service", and the domain layer has no `list-venues.service.ts` or `get-venue.service.ts`. This also forced the `venueRepository` and `doorSaleTierRepository` to be injected into the `services` container (`graphql.types.ts:22,29`), mixing repositories and services in the same namespace | `backend/src/graphql/handlers/venues/list-venues.handler.ts:11-14`, `get-venue.handler.ts:11-14`, `backend/src/graphql/graphql.types.ts:22,29` | Add `list-venues.service.ts` and `get-venue.service.ts` (or a single `get-venues.service.ts` with `listAll`/`getById`) that the handlers delegate to; rename the context field from `services` to something honest (e.g. split into `services` and `repositories`) if repo injection is kept | M |
| 🔴 | Events CRUD | Pattern: Data access layer — the `Event.createdBy` type resolver in `resolvers/index.ts` constructs a raw Drizzle `select()...from(user)...` query inside the resolver file, bypassing `userRepository` entirely; patterns.md mandates "Data access layer (one per entity)" and resolvers must go through repositories to keep Drizzle isolated | `backend/src/graphql/resolvers/index.ts:53-60` | Add a `findById` method to `UserRepository` and inject it via context; call `context.services.userRepository.findById(parent.createdBy)` from the resolver instead of hand-rolling a Drizzle query in the resolver file | S |
| 🟡 | Events CRUD | Pattern: Service pattern — Zod validation is applied inconsistently across services: `create-event.service.ts:51` uses `schema.parse()` (throws raw `ZodError`), `create-venue.service.ts:25-29` uses `safeParse` and concatenates per-field errors, `manage-tiers.service.ts:32-34,59-61` uses `safeParse` but throws a hardcoded message; patterns.md says "Validates input with Zod schemas" but doesn't prescribe failure shape, so this drift is likely to propagate | `backend/src/domain/events/create-event.service.ts:51`, `create-venue.service.ts:25-29`, `manage-tiers.service.ts:32-34`, `update-event.service.ts:49` | Pick one convention (`safeParse` + a canonical thrown error) and apply uniformly; document in patterns.md under Service Pattern so future services don't re-invent | M |
| 🟡 | Events CRUD | Pattern: Handler error handling — drift within Events CRUD: `create-event.handler.ts:37`, `update-event.handler.ts:39`, `delete-event.handler.ts:18`, `get-event.handler.ts:15`, `list-events.handler.ts:14`, `transition-event.handler.ts:19` use `throw error instanceof Error ? error : new Error(String(error))`; `add-tier.handler.ts:18`, `update-tier.handler.ts:18`, `remove-tier.handler.ts:15` use `throw new Error(error instanceof Error ? error.message : "Failed to X")` (fallback strings lose the original error); `create-venue.handler.ts:27` uses a third shape with `error: any`; patterns.md is silent | `backend/src/graphql/handlers/events/*.handler.ts` and `backend/src/graphql/handlers/venues/create-venue.handler.ts:22-28` | Document a single error-handling convention in patterns.md (e.g. "handlers do NOT catch — let GraphQL Yoga surface the error; services throw `Error` with user-facing messages") and apply uniformly; the `try/catch/console.error/rethrow` in every handler is effectively a log-and-rethrow wrapper that adds no value | S |
| 🟡 | Events CRUD | Pattern: Handler auth gating — every handler re-implements `if (!context.user) throw new Error("Authentication required")` as its first line (9 event handlers + 3 venue handlers + 2 tier handlers = 14 copies); patterns.md defines "Receives GraphQL args and context" but does not document the auth-guard pattern. Besides the repetition, the thrown error has no `extensions.code: UNAUTHENTICATED` — same root cause as the Auth audit finding about urql's `didAuthError` never firing | `backend/src/graphql/handlers/**/*.handler.ts` (14 files) | Introduce a `requireAuth(context)` helper (or a `withAuth(handler)` wrapper) in `graphql/handlers/common/` that throws a typed `GraphQLError` with `extensions: { code: 'UNAUTHENTICATED' }`; document in patterns.md | S |
| 🟡 | Events CRUD | Pattern: Resolver wiring — `resolvers/index.ts` currently mixes flat `Query`/`Mutation` maps with a nested `Event` type-resolver object that also embeds business logic (the `Event.createdBy` Drizzle query, see 🔴 above); patterns.md says "Keep flat — no nested resolver objects unless needed for type resolution" which permits this but doesn't describe where type-resolver code should live when it grows. Today the file is 62 lines; at 5+ entities this will bloat | `backend/src/graphql/resolvers/index.ts:45-61` | Document a convention in patterns.md: "type resolvers for entity X live in `graphql/resolvers/<entity>.resolver.ts` and are re-exported from `resolvers/index.ts`"; extract `Event` resolvers to `event.resolver.ts` as the first application | S |
| 🟡 | Events CRUD | Pattern: Repository — soft-delete policy is inconsistent across repositories. `event.repository.ts:60,70` filters `deleted = false` in `findById`/`findByCreator`; `user.repository.ts` has no analogous filter even though `user.deleted` exists in the schema; `door-sale-tier.repository.ts` and `venue.repository.ts` have no soft-delete columns at all. Patterns.md is silent on soft-deletes, so the next entity will have to guess | `backend/src/repositories/event.repository.ts:60,70`, `user.repository.ts:37-57`, `backend/src/db/schema.ts:15,43` | Document in patterns.md the soft-delete convention ("repositories filter `deleted = false` in all read paths; hard-delete is only for transient entities"); audit `user.repository.ts` reads to apply the filter | M |
| 🟡 | Events CRUD | Pattern: Multi-table writes — `create-event.service.ts:58-74` performs two sequential `await` statements against two repositories (event insert, then event-team-member insert) without a transaction; same service also clearly SHOULD be transactional per the spec (see 🔴 BR-1 row already in this table). Patterns.md is silent on transactions, so this will be re-litigated whenever a new multi-table write lands (guest lists, etc.) | `backend/src/domain/events/create-event.service.ts:58-74` | Add a Transactions section to patterns.md: "Services that write to more than one table MUST wrap the writes in `db.transaction(async (tx) => {...})`; repositories must accept an optional `tx` parameter, defaulting to `this.db`." Refactor `event.repository.ts` + `event-team-member.repository.ts` to accept a `tx`, then wrap the create-event service in a transaction | L |
| 🟡 | Events CRUD | Pattern: Service pattern — services use a mixed `run(input)` (`CreateEventService`, `UpdateEventService`, `TransitionEventService`, `DeleteEventService`, `CreateVenueService`) vs explicit method names (`GetEventsService.getById`/`listByCreator`, `ManageTiersService.addTier`/`updateTier`/`removeTier`); patterns.md's Service Pattern prescribes "One file per use case" but doesn't say how many methods a service may expose. `GetEventsService` and `ManageTiersService` each cover multiple use cases, which either violates "one file per use case" or signals that the rule needs a "related reads/writes may share a service" exception | `backend/src/domain/events/get-events.service.ts`, `manage-tiers.service.ts` | Clarify in patterns.md: "A service file may expose multiple methods when they share the same aggregate and authorization rules; otherwise one method named `run()`"; or split `ManageTiersService` into `add-tier.service.ts`, `update-tier.service.ts`, `remove-tier.service.ts` to follow the `<verb>.service.ts` convention literally | S |
| 🟡 | Events CRUD | Pattern: Handler args typing — inconsistent `_parent`/`_args` typings: events handlers use `_parent: unknown` (typed), venue handlers use `_parent: any` + `_args: any`; within events, `update-tier.handler.ts:5` declares `args: { id: string; input: {...} }` inline, while `create-event.handler.ts:4-16` spells out the `input` shape fully. Patterns.md is silent on typing | `backend/src/graphql/handlers/venues/*.handler.ts:12,6-7` and `backend/src/graphql/handlers/events/*.handler.ts` | Document in patterns.md: "handlers type `_parent: unknown`, `args` with the operation's input shape, and `context: AppGraphQLContext`; no `any`". Align venue handlers to the events pattern | S |
| 🟡 | Events CRUD | Pattern: Context namespace — `AppGraphQLContext.services` mixes actual services (`createEventService`, `manageTiersService`) with repositories (`venueRepository`, `doorSaleTierRepository`) because of the service-layer bypass noted in the venue-handler row above; the field name no longer matches its contents, and new readers will wonder whether repositories are supposed to be in context at all | `backend/src/graphql/graphql.types.ts:17-30` | Split into `AppGraphQLContext.services` and `AppGraphQLContext.repositories` (or fold repositories behind new services); document the context-shape convention in patterns.md | S |
| 🟡 | Events CRUD | Pattern: Frontend screen self-containment — `create.tsx` (475 LOC) and `edit.tsx` (564 LOC) duplicate the venue-picker Modal verbatim (lines 365-472 in create.tsx ≈ lines 454-561 in edit.tsx), plus the inline create-venue form and all its state (`newVenueName`, `newVenueAddress`, `newVenueCapacity`, `showCreateVenue`, `venueSearch`, `venueModalVisible`). Patterns.md does not mention component extraction, so screen files will keep growing | `rn-app/app/(app)/events/create.tsx:364-473` and `rn-app/app/(app)/events/[id]/edit.tsx:453-562` | Extract a `VenuePickerModal` component into `rn-app/components/ui/` (or `rn-app/components/events/`) accepting `visible`, `onSelect`, `onClose` props; document in patterns.md: "Screens stay under ~300 LOC; shared modals/forms live in `components/`" | M |
| 🟡 | Events CRUD | Pattern: Frontend typing — pervasive `any` in `create.tsx:37,70,92,116,390,392`, `[id]/edit.tsx:57,99,112,124,125,194,212,226,479,481`, `[id]/index.tsx:212`, and `resolvers/index.ts:46,50,53`; patterns.md is silent on strictness. Urql operations are untyped, so every `result.data?.createEvent` etc. is `any`, masking the wrong-variable-name bugs already flagged in Task 4 | `rn-app/app/(app)/events/**/*.tsx` | Document in patterns.md: "Frontend code uses urql's generated types (consider `@graphql-codegen/cli` or `graphql-request`) — `any` is not allowed in `*.tsx` screens." Generate types from the schema and refactor | L |
| 🟢 | Events CRUD | New pattern: Drizzle schema conventions — columns use `snake_case` DB names with `camelCase` TS properties (`door_sales_enabled` ↔ `doorSalesEnabled`, `location_name` ↔ `locationName`, etc.); primary keys are `varchar(24)` with `$defaultFn(() => createId())`; booleans use `.default(false).notNull()`; timestamps use `.defaultNow().notNull()`; enums live at the top of `schema.ts`. Consistent but undocumented; newcomers will have to reverse-engineer | `backend/src/db/schema.ts:1-65` | Document Drizzle conventions in patterns.md under a new "Database Patterns" section (column naming, id/primary-key shape, default/null defaults, enum placement) | S |
| 🟢 | Events CRUD | New pattern: Entity files re-export `InferSelectModel` as `<Entity>Schema` alongside domain `<Entity>Entity` interfaces — `event.entity.ts:4`, `venue.entity.ts:4`, `door-sale-tier.entity.ts:4`, `event-team-member.entity.ts:4`; patterns.md doesn't describe this split (domain Entity vs DB Schema) | `backend/src/repositories/event.entity.ts:4-24`, `venue.entity.ts:4-14`, `door-sale-tier.entity.ts:4-12`, `event-team-member.entity.ts:4-14` | Document in patterns.md: "Entity files expose (a) `XSchema = InferSelectModel<typeof x>` for internal repo use and (b) a domain-level `XEntity` interface for handlers/services; repositories map Schema → Entity" | S |
| 🟢 | Events CRUD | New pattern: `mapToEntity` helper convention — every repository has a file-private `mapToEntity(row: XSchema): XEntity` function near the top (`event.repository.ts:14-32`, `venue.repository.ts:12-22`, `door-sale-tier.repository.ts:14-22`, `event-team-member.repository.ts:11-19`); patterns.md is silent on the mapping convention | `backend/src/repositories/*.repository.ts` | Document in patterns.md: "Each repository exposes a file-private `mapToEntity(row: XSchema): XEntity` used by every read method; add fields here rather than in the query builder" | S |
| 🟢 | Events CRUD | New pattern: Soft-delete columns follow a `deleted: boolean` + nullable `deletedAt: timestamp` shape (user + event tables); door-sale-tier and event-team-member are hard-deleted; patterns.md is silent on when to soft-delete vs hard-delete | `backend/src/db/schema.ts:15-18,43-46` | Document in patterns.md: "Aggregate roots (user, event, venue) soft-delete; owned child rows (door-sale-tier, event-team-member) hard-delete with the aggregate" (or whatever policy the team picks) | S |

### Cross-cutting

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|

### Doc currency

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|

### Agent skills

| Severity | Area | Description | Location | Recommended action | Effort |
|----------|------|-------------|----------|--------------------|--------|
